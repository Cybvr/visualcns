import type { Metadata } from "next"
import type { ReactNode } from "react"

export const metadata: Metadata = { title: "Edit Estimate" }

export default function EditEstimateLayout({ children }: { children: ReactNode }) {
  return children
}
