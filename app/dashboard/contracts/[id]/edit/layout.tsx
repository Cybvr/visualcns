import type { Metadata } from "next"
import type { ReactNode } from "react"

export const metadata: Metadata = { title: "Edit Contract" }

export default function EditContractLayout({ children }: { children: ReactNode }) {
  return children
}
